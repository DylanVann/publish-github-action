name: 'Publish GitHub Action'
on:
  push:
    branches:
      - master

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: dylanvann/publish-github-action@v1.1.33
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
